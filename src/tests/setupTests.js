/* eslint-disable no-undef */
/**
 * Jest setup file — runs after the test framework is installed.
 * Extends expect() with custom DOM matchers from @testing-library/jest-dom.
 */
import '@testing-library/jest-dom';
